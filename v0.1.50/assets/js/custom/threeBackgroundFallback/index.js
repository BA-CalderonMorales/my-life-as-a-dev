/**
 * Three Background Fallback Module Index
 * Main export file for the CSS fallback for THREE.js background
 */
import BackgroundFallback from './BackgroundFallback.js';

// Create and export the default instance
const backgroundFallback = new BackgroundFallback();

export default backgroundFallback;
