/**
 * Section Transitions Module Index
 * Main export file for the section transitions system
 */
import SectionTransitions from './SectionTransitions.js';

// Create and export the default instance
const sectionTransitions = new SectionTransitions();

export default sectionTransitions;
